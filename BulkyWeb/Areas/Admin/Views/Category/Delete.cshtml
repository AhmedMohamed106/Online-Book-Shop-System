@model Bulky.Models.Models.Category

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient m-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2"> Confirm Delete</h2>

            </div>
        </div>

    </div>
    <div class="card-body p-4">
        <p>Are you sure you want to delete the category: @Model.Name?</p>

        @using (Html.BeginForm("Delete", "Category", new { Id = Model.Id }, FormMethod.Post))
        {
            <input type="submit" value="Delete" class="btn btn-danger" />
                    <a asp-action="Index" class="btn btn-secondary" style="background-color: #CCCCCC; color:black">Cancel</a>
        }

        </div>
                </div>



@*
@model Category


<div class="container">
    <h2>Edit Category</h2>

    <form asp-controller="Category" asp-action="Delete" method="post">
        <div as="form-group" class="border p-3 mt-4">
            <div class="row p-1">
            <label  asp-for="Name" for="name" class="control-label p-0"></label>
            <input asp-for="Name"  id="name" class="form-control"  disabled  />
        </div >
            <div class="row p-1">
            <label asp-for="DisplayOrder" for="order" class="control-label p-0"></label>
            <input asp-for="DisplayOrder" id="order" class="form-control"  disabled />

           </div>
        </div>

        <div class="form-group">
            <button type="submit" class="btn btn-primary" style="width:150px; margin-top:15px">Delete</button>
            <a href="@Url.Action("Index", "Category")" class="btn btn-secondary border form-control" style="width:200px; margin-top:15px; margin-left:30px;">Back to List</a>
        </div>
    </form>


</div>
*@